-# FR ESS 배터리 열-전기 및 수명 열화 모델링 워크플로우

본 워크플로우는 2편의 참고 논문(Assessing Geographical and Seasonal Influences... 등)에 제시된 수냉식 트럭 배터리 팩 모델을 기반으로, **KEPCO FR ESS 공랭식 환경과 사용자의 HPPC 실험 데이터를 결합하여 현장에 최적화된 마스터 열화 추정 시스템을 구축하는 방법론**을 설명합니다.

---

## 🚀 워크플로우 요약 (하이브리드 접근법)
1. **[실험실, Lab]** HPPC 데이터를 활용하여 단일 셀의 전기(Electrical) 및 고유 열(Thermal) 파라미터를 정확히 추출 ($R_0, R_1, C_1, C_{cell}, R_{housing,cell}$)
2. **[현장, Field]** 추출된 셀 파라미터를 고정값으로 두고, FR ESS의 거대 공조(HVAC) 환경 데이터(PLC, RBMS)를 활용하여 시스템 덩어리의 열 파라미터를 역산 추출 ($C_{housing}, R_{amb,housing}$)
3. **[평가, Aging]** 완성된 FR ESS 전용 열-전기 디지털 트윈 모델에 현장의 충방전 프로파일을 통과시켜, 누적 사이클 및 캘린더 수명 열화(SOH 저하)를 지속적으로 추적 및 예측

---

## 단계 1. [Lab] HPPC 데이터를 통한 단일 셀 파라미터 추출 (Calibration)

실험실 챔버 내에서 수행된 정밀한 HPPC(Hybrid Pulse Power Characterization) 실험 데이터를 바탕으로 논문의 전기 모델 식(Eq. 7, 8) 및 셀 열 모델 식(Eq. 10 혹은 Eq. 23)을 튜닝합니다.

### 1.1 입력 데이터 (HPPC Test)
*   $t$: 시간 (Time)
*   $I_{cell}$: 인가된 충전/방전 펄스 전류 (Current)
*   $V_{cell}$: 펄스에 의한 전압 응답 (Voltage)
*   $T_{cell}$: 펄스 및 휴지기 동안의 셀 겉면 온도 응답 (Cell Surface Temperature)
*   $T_{amb\_chamber}$: 항온항습기 설정 온도 (Chamber Temperature)

### 1.2 전기 모델 파라미터 추출 ($R_0, R_1, C_1$)
*   **방법론:** 전류 펄스 인가 직후의 순간적인 전압 강하량($\Delta V$)을 통해 고주파 저항 $R_0$ 획득
*   **방법론:** 펄스 지속 시 지수함수적으로 변하는 전압 곡선에 분극 저항 및 커패시턴스 함수를 커브 피팅(Curve-fitting)하여 $R_1, C_1$ 획득
*   **최종 결과물:** 각 SOC(State of Charge) 구간별 $R_0, R_1, C_1$ 룩업 테이블(Look-up Table)

### 1.3 셀 단위 열 파라미터 추출 ($C_{cell}, R_{housing,cell}$)
*   **원리:** 전류 통전으로 발생한 전기적 손실(Joule Heating)이 열이 되어 셀의 온도를 올리고($C_{cell}$), 주변 챔버 공기로 식어가는($R_{housing,cell}$ 또는 $R_{amb,cell}$) 비율을 계산
*   **방법론:** MATLAB의 `fmincon` 또는 `particleswarm` 최적화 툴을 사용하여, 시뮬레이션 방정식(Eq.23, $\frac{dT_{cell}}{dt}$)이 내뿜는 모의 온도 곡선이 **실제 HPPC $T_{cell}$ 실험 곡선과 완벽히 오버랩되도록** 하는 $C$와 $R$ 상수를 탐색
*   **최종 결과물:** 단일 셀 고유의 열상수 $C_{cell}$ (예: 158.6 J/K), $R_{housing,cell}$ 확정

---

## 단계 2. [Field] FR ESS 공랭식 특화 열 모델 파라미터 추출 (Calibration)

위 1단계에서 찾은 **셀 파라미터를 상수로 고정**하고, 현장 데이터에 논문의 팩 하우징 열 모델 (Eq. 24)을 FR ESS 맞춤형으로 변형(Modified Eq. 24)하여 스케일업 파라미터를 찾아냅니다.

### 2.1 현장 데이터 변수 매칭
*   $I_{cell}$ $\rightarrow$ **RBMS `DC Current(A)`** / (모듈 내 병렬 셀 개수)
*   $V_{cell}$ $\rightarrow$ **RBMS `Average C.V.(V)`**
*   $T_{housing}$ $\rightarrow$ **RBMS `Average M.T.(oC)`** (모듈 외함 표면 온도 측정값)
*   $T_{amb}$ $\rightarrow$ **PLC `HVAC1_RoomTemp.`** (에어컨으로 시원해진 컨테이너 룸 내부 공기 온도)
*   $\dot{Q}_{TMS}$ $\rightarrow$ **0 kW** (FR ESS는 논문의 트럭과 달리 팩에 꽂히는 수냉식 쿨런트 배관 에너지가 없음. 냉각 효과는 이미 `HVAC1_RoomTemp` 센서값 하락에 반영되어 있음)
*   $N_{cells}$ $\rightarrow$ RBMS 측정 단위가 "모듈"이므로, 1개 모듈 안에 들어있는 총 직병렬 셀의 개수로 축소하여 입력 (예: 28개)

### 2.2 현장 시스템 열 파라미터 추출 ($C_{housing}, R_{amb,housing}$)
*   **타겟 방정식:** 
    $C_{housing} \frac{dT_{housing}}{dt} = N_{cells} \cdot \frac{T_{cell} - T_{housing}}{R_{housing,cell}} + \frac{T_{room} - T_{housing}}{R_{room,housing}} + (0)$
*   **최적화 목표:** 임의의 $C_{housing}$ 과 $R_{room,housing}$ 을 넣어 시뮬레이션한 가상의 $T_{housing}$ 결과 곡선이, 현장 실제 **`Average M.T.` 곡선과 Root-Mean-Square Error(RMSE)가 최소가 되도록** 알고리즘 구동.
*   **최종 결과물:** 
    *   $C_{housing}$: 모듈(또는 랙) 케이스 전체를 달구는 열용량 상숫값 도출
    *   $R_{amb,housing}$: 모듈 표면에서 에어컨 찬공기로 열을 뺏기는 열저항 상숫값 도출

---

## 단계 3. [Aging] 배터리 수명 저하 (SOH) 시뮬레이션

완벽히 조립된 전기-열 결합 방정식 트리(디지털 트윈)를 바탕으로, 논문의 두 가지 이원화된 노화(Aging) 모델 파이프라인을 가동합니다.

### 3.1 Cycling Aging (운전 시 용량 감소)
*   **데이터 필터 주건:** `DC Current(A)` $\neq 0$ 인 동적 구간
*   **연산 로직:**
    1. 측정 전류를 초단위로 적분하여 누적 통전량 (Ah-throughput) 누적 연산
    2. C-rate 환산: `DC Current` / 팩 Rated Capacity
    3. 단계 2에서 완성된 열 시뮬레이션으로 얻어진 정밀 코어 온도($T_{cell}$) 정보 조달
    4. 논문의 Eq 30, Eq 44 수식에 대입하여 사이클 단위별 열화 증가량(+) 누적
*   **현장 피팅 팁:** 논문의 모델 파라미터 보다는 KEPCO 현장 SOH 진단 데이터를 Ground Truth로 삼아, 공식(Eq 44)의 승수나 계수를 MLR / Random Forest 로 튜닝/보정 권장.

### 3.2 Calendar Aging (대기/휴지 시 용량 감소)
*   **데이터 필터 조건:** `DC Current(A)` $\approx 0$ 인 주파수 조정 대기 구간(Resting period)
*   **연산 로직:**
    1. 쉬는 구간이 시작될 때의 방치된 충전전압 상태: `SOC(%)` 
    2. 쉬는 구간 동안의 모듈 평균 온도 상태: `Average M.T.`
    3. 쉬는 총 시간 ($t_{store}$)
    4. 논문의 Eq 32, Eq 45 에 대입하여 휴지기 단위별 자연 노화 증가량(+) 누적

### 3.3 최종 통합 (SOH Simulation)
*   Total Capacity Fade (%) = Cycling Aging Fade (%) + Calendar Aging Fade (%)
*   `pred_soh` = 100% - Total Capacity Fade
*   위 파이프라인을 1월부터 돌리며 연장시키면, "어떤 달, 어떤 수준의 공조 온도" 가 배터리 SOH 하락에 가장 치명적이었는지 정량적으로 분석 가능.
